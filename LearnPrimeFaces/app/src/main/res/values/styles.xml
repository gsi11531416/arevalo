<resources>

    <style name="Theme.PrimeFaces" parent="Base.AppTheme" />

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>

        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.ConNavigationDrawer" parent="Base.AppTheme"/>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>


    <style name="TemaKillDead" parent="android:style/Theme.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="TextoKillDead.Botones" >
        <item name="android:textSize">@dimen/textsizebtnmain</item>
        <item name="android:padding">@dimen/padding_txt_btn</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>

    </style>

    <style name="Botones" parent="@android:style/Widget.Button.Small">
        <item name="android:textAppearance">@style/TextoKillDead.Botones</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textColor">#00DD00</item>
        <item name="android:layout_margin">@dimen/margin_btn</item>
        <item name="android:background">@drawable/maderabg</item>
    </style>

    <style name="TextoKillDead" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">@dimen/textsizetitulomain</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#00FF00</item>
        <item name="android:typeface">monospace</item>
        <item  name="android:gravity">center</item>
    </style>
</resources>
